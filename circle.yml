
machine:
  node:
    version: 5
  php:
    version: 7.0.4
test:
  post:
    - php pagekit build
    - mv pagekit-*.zip $CIRCLE_ARTIFACTS
dependencies:
  cache_directories:
    - "app/vendor"
    - "packages"

