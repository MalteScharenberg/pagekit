
machine:
  node:
    version: 5
  php:
    version: 7.0.4
dependencies:
  post:
    - php pagekit build
test:
  post:
    - mv pagekit-*.zip $CIRCLE_ARTIFACTS
dependencies:
  cache_directories:
    - "app/vendor"
    - "packages"

